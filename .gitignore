.venv/
venv/
.env
results.json
prospects.csv

# Gmail OAuth secrets — never commit (see docs/gmail-setup.md)
secrets/
credentials.json
token.json
__pycache__/
*.pyc
.pytest_cache/

# SQLite store (+ WAL/SHM sidecar files)
prospector.db
prospector.db-wal
prospector.db-shm

# Unattended populate driver: log + lock
populate.log
.populate.lock

# Editor / IDE
.idea/
.vscode/
*.swp

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
.claude/*.tmp.*
